---
name: Update streams

on:
  push:
    branches:
      - master
      - update-script
    paths:
      - source.yaml

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install kodinerds-iptv tool
        run: pipx install .

      - name: Update IPTV lists
        run: kn-iptv generate --output-dir iptv --logo_base_path \
          https://raw.githubusercontent.com/jnk22/kodinerds-iptv/master/logos/ \
          sources/*.yaml

      - name: Commit changes
        uses: EndBug/add-and-commit@v9
        with:
          author_name: Update Bot
          author_email: update-bot@example.com
          message: "Update .m3u files"
          add: "iptv/*.m3u"
